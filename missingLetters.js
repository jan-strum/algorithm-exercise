const findMissingLetters = str => {
  if (typeof str !== 'string') {
    throw new TypeError('findMissingLetters must take a string as an input.')
  }

  const alphabet = 'abcdefghijklmnopqrstuvwxyz'
  return ''
}

module.exports = findMissingLetters
